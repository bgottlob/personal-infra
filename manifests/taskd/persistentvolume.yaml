apiVersion: v1
kind: PersistentVolume
metadata:
  name: taskd-storage
spec:
  storageClassName: linode-block-storage-retain
  capacity:
    storage: 1Gi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: /data/taskd
    type: DirectoryOrCreate
