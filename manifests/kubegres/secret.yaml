apiVersion: v1
kind: Secret
metadata:
  name: mypostgres-secret
  namespace: postgres
type: Opaque
stringData:
  superUserPassword: "${POSTGRES_SUPER_USER_PASSWORD}"
  replicationUserPassword: "${POSTGRES_REPLICATION_USER_PASSWORD}"
