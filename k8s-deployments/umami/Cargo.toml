[package]
name = "umami"
version = "0.1.0"
description = "Yoke flight for umami"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
k8s-openapi = { version = "0.26.0", features = ["latest"] }
serde_json = "1.0.145"
kube-builder = { path = "../kube_builder" }
